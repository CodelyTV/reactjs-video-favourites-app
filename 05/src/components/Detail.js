import React, {Component} from 'react';
import { withRouter } from 'react-router'

class Detail extends Component {
  constructor(props) {
    super(props);
  }
  render() {
  
    return (<React.Fragment> Vista Detalle </React.Fragment>);
  }
}

export default  withRouter(Detail);